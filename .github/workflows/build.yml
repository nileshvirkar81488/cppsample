name: SonarQube
on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  build:
    name: Build and analyze
    runs-on: ubuntu-latest
    env:
      BUILD_WRAPPER_OUT_DIR: build_wrapper_output_directory # Directory where build-wrapper output will be placed
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      - name: Install Build Wrapper
        uses: SonarSource/sonarqube-scan-action/install-build-wrapper@v5
      - name: Run Build Wrapper
        run: |
            # Navigate to the cppfiles directory where CMakeLists.txt is located
            cd cppfiles
            
            # Create a directory for the build
            mkdir build && cd build

            # Run CMake to generate the Makefile in the 'build' directory
            # The ".." points to the cppfiles directory containing CMakeLists.txt
            cmake ..
            
            # Run the build with build-wrapper (using absolute path for output)
            build-wrapper-linux-x86-64 --out-dir ${GITHUB_WORKSPACE}/${{ env.BUILD_WRAPPER_OUT_DIR }} make -j$(nproc)
      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            --define sonar.cfamily.compile-commands="${{ env.BUILD_WRAPPER_OUT_DIR }}/compile_commands.json"
